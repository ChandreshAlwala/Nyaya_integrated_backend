services:
  - type: web
    name: nyaya-backend
    env: python
    buildCommand: pip install -r minimal_requirements.txt
    startCommand: python robust_legal_query.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0